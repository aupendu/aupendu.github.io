{
    "configurations": [
    {
        "name": "Launch Publications in Chrome",
        "request": "launch",
        "type": "chrome",
        "url": "file://${workspaceFolder}/publications.html",
        "webRoot": "${workspaceFolder}"
    }
    ]
}